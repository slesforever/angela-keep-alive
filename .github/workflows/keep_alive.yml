name: Angela Keep Alive

on:
  schedule:
    - cron: '*/5 * * * *'  # 每 5 分鐘固定 ping 一次
  workflow_dispatch:       # 允許手動測試

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send Ping
        run: |
          curl -I https://angela-keep-alive.onrender.com/
